name: daily-country-short

# Countries pipeline: 3 countries per day, ONE video per run, firing every 8 hours
# (00:00, 08:00, 16:00 UTC). Narration generated by OpenAI GPT-4o-mini, footage
# from Pexels/Pixabay, assembled with ffmpeg, posted via Google Sheet → Zapier.
on:
  schedule:
    - cron: "0 */8 * * *"   # fires at 00:00, 08:00, 16:00 UTC -> 3 videos/day, 1 per run
  workflow_dispatch:
    inputs:
      country_index:
        description: "Force a specific country index (blank = auto date+time-slot rotation)"
        type: string
        default: ""
      footage_source:
        description: "Force footage source (blank = auto: pexels -> pixabay -> animate)"
        type: choice
        options: ["", "pexels", "pixabay", "animate", "archive"]
        default: ""

permissions:
  contents: write          # commit logs/history.csv and create the release asset

jobs:
  build_and_post:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install system deps (ffmpeg, fonts, espeak fallback)
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg fonts-dejavu-core espeak-ng

      - name: Install python deps
        run: pip install -r requirements.txt

      # ---- content pipeline ----
      # Which of today's 3 runs is this? 00:00 UTC -> slot 0, 08:00 -> slot 1, 16:00 -> slot 2.
      - name: Generate country script (OpenAI GPT-4o-mini)
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          IDX_ARG=""
          if [ -n "${{ inputs.country_index }}" ]; then
            IDX_ARG="--index ${{ inputs.country_index }}"
          else
            HOUR=$(date -u +%H)
            SLOT=$(( 10#$HOUR / 8 ))
            IDX_ARG="--slot $SLOT"
          fi
          python scripts/generate_country_script.py \
            --config config/countries.json $IDX_ARG

      - name: Fetch footage (Pexels / Pixabay / animation)
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          PIXABAY_API_KEY: ${{ secrets.PIXABAY_API_KEY }}
        run: |
          ARG=""
          if [ -n "${{ inputs.footage_source }}" ]; then
            ARG="--source ${{ inputs.footage_source }}"
          fi
          python scripts/fetch_footage.py --config config/countries.json $ARG

      - name: Generate TTS voiceover
        # Tries OpenAI TTS first (natural voice), then Piper, then espeak-ng —
        # all three are attempted inside one call before it gives up.
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python scripts/generate_tts.py --config config/countries.json

      - name: Generate captions
        run: python scripts/generate_captions.py --config config/countries.json

      - name: Generate background music (ambient pad)
        run: python scripts/generate_music.py

      - name: Assemble video
        run: python scripts/assemble_video.py --config config/countries.json

      # ---- make the video publicly fetchable (Zapier / Instagram Reels needs a public URL) ----
      # NOTE: release assets are only PUBLICLY reachable if this repo is PUBLIC.
      # For a private repo, host final.mp4 elsewhere and set the URL accordingly.
      - name: Publish video as release asset
        id: publish_asset
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="daily-$(date -u +%Y%m%d-%H%M%S)"
          gh release create "$TAG" build/final.mp4 \
            --title "$TAG" --notes "Automated short (runs every 8h, 3/day)" --latest=false
          URL="https://github.com/${GITHUB_REPOSITORY}/releases/download/${TAG}/final.mp4"
          echo "video_url=$URL" >> "$GITHUB_OUTPUT"
          echo "Public video URL: $URL"

      # ---- posting: append one row to the shared Google Sheet; Zapier does the rest ----
      # One Zap per platform (Instagram / Facebook / YouTube) watches this sheet with a
      # free "New Spreadsheet Row" trigger and posts via Zapier's own connections. No
      # platform tokens are stored in this repo.
      - name: Post via Google Sheet (Zapier)
        id: post_sheet
        continue-on-error: true
        env:
          SHEET_WEBHOOK_URL: ${{ secrets.SHEET_WEBHOOK_URL }}
          SHEET_WEBHOOK_TOKEN: ${{ secrets.SHEET_WEBHOOK_TOKEN }}
        run: |
          python scripts/post_sheet.py \
            --video-url "${{ steps.publish_asset.outputs.video_url }}" \
            --script build/script.json \
            --caption-file build/caption_meta.txt \
            --config config/countries.json

      # ---- posting: TikTok via Zernio (https://zernio.com) ----
      # Independent of the Sheet/Zapier flow above — Zapier has no free TikTok posting
      # integration, so TikTok goes through Zernio's own API instead. Safe by default:
      # lands as a Creator Inbox draft the connected account owner taps to publish,
      # rather than posting live. Silently skipped if the Zernio secrets aren't set.
      - name: Post to TikTok (Zernio)
        id: post_tiktok
        continue-on-error: true
        env:
          ZERNIO_API_KEY: ${{ secrets.ZERNIO_API_KEY }}
          ZERNIO_PROFILE_ID: ${{ secrets.ZERNIO_PROFILE_ID }}
          ZERNIO_TIKTOK_ACCOUNT_ID: ${{ secrets.ZERNIO_TIKTOK_ACCOUNT_ID }}
        run: |
          python scripts/post_zernio_tiktok.py \
            --video-url "${{ steps.publish_asset.outputs.video_url }}" \
            --script build/script.json \
            --caption-file build/caption_tiktok.txt

      # ---- log results back to the repo ----
      - name: Append run to history log
        if: always()
        run: |
          DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          SCRIPT_TITLE=$(python -c "import json;d=json.load(open('build/script.json'));print(d.get('name',d.get('title','unknown')))" 2>/dev/null || echo "unknown")
          IDENT=$(python -c "import json;print(json.load(open('build/footage.json'))['identifier'])" 2>/dev/null || echo "unknown")
          echo "${DATE},\"${SCRIPT_TITLE}\",${IDENT},${{ steps.post_sheet.outcome }}" >> logs/history.csv
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add logs/history.csv
          git commit -m "log: run ${DATE}" || echo "nothing to commit"
          git push
